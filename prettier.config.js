//  @ts-check

/** @type {import('prettier').Config} */
const config = {
   tabWidth: 3,
   semi: false,
   trailingComma: "es5",
   printWidth: 100,
   bracketSpacing: true,
   arrowParens: "avoid",
   proseWrap: "preserve",
}

export default config
